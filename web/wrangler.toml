name = "stockresearch"
compatibility_date = "2024-09-01"
pages_build_output_dir = "./dist"

# 在 Cloudflare Dashboard → Settings → Environment variables 配置：
# BACKEND_URL = https://你的后端域名   （不要填 LLM_API_KEY）
